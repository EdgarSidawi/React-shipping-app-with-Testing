import React from 'react';

import classes from './Spinner.module.css';

const spinner = () => {
  return (
    <div className={classes.spinner}>
      .row .col-sm-2.col-xs-4.text-center .spinner-loader
    </div>
  );
};

export default spinner;
